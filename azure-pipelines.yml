trigger:
  - master

pool:
  name: 'Default' # Specify the name of your agent pool here

steps:

- checkout: self

- script: |
    "C:\Program Files\Parasoft\SOAtest & Virtualize\2023.2\soatestcli.exe" -import "$(Build.SourceDirectory)/s" -data "$(System.DefaultWorkingDirectory)\workspace"
    "C:\Program Files\Parasoft\SOAtest & Virtualize\2023.2\soatestcli.exe" -data "$(System.DefaultWorkingDirectory)\workspace" -config "soatest.user://Example Configuration" -report "$(System.DefaultWorkingDirectory)"

  displayName: 'Execute soatestcli'

- task: PublishParasoftResults@1
  inputs:
    resultsFiles: |
      **/rep*.xml
      **/rep*.sarif
      **/coverage.xml


